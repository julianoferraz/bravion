# ============================================
# Variáveis de Ambiente — Bravion Global
# ============================================
# Copie este arquivo para .env e preencha os valores
# NUNCA commit o arquivo .env com valores reais!
# ============================================

# ==========================================
# SUPABASE (Frontend)
# ==========================================
# URL do projeto Supabase (ex: https://abc123.supabase.co)
VITE_SUPABASE_URL=

# Chave pública (anon key) do Supabase — segura para expor no frontend
VITE_SUPABASE_PUBLISHABLE_KEY=

# ID do projeto Supabase (ex: abc123)
VITE_SUPABASE_PROJECT_ID=

# ==========================================
# SUPABASE SECRETS (Backend / Edge Functions)
# Configurar via: supabase secrets set NOME=VALOR
# ==========================================

# LOVABLE_API_KEY — Chave para API de IA (geração de texto/imagem)
# Para migração, substituir por:
#   - Google AI Studio: https://aistudio.google.com (gerar API Key)
#   - OpenAI: https://platform.openai.com (gerar API Key)
# Configurar via: supabase secrets set LOVABLE_API_KEY=sua_chave

# SUPABASE_SERVICE_ROLE_KEY — Chave admin do Supabase (configurada automaticamente)
# Usada pela Edge Function publish-scheduled

# SUPABASE_URL — URL do Supabase (configurada automaticamente)
# SUPABASE_ANON_KEY — Chave anônima (configurada automaticamente)
# SUPABASE_DB_URL — URL de conexão direta ao PostgreSQL (configurada automaticamente)
